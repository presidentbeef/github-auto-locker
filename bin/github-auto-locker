#!/usr/bin/env ruby
require_relative '../lib/locker'

noop = ARGV.delete "-n"

abort "Usage: auto-lock [user] [repo] [token] [days]" unless ARGV[2]

Locker.new(ARGV[0], ARGV[1], ARGV[2], ARGV[3] || 120, noop).lock
